<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBackground">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:title="Attendance"
            app:titleCentered="true" />
    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/appBarLayout"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Mark Attendance"
            android:textColor="@color/textColorPrimary"
            android:textSize="28sp"
            android:textStyle="bold" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_mark_by_qr"
            style="@style/AdminCardView"
            android:layout_marginTop="32dp">

            <RelativeLayout style="@style/Widget.App.CardContent">
                <ImageView style="@style/Widget.App.CardIcon" android:src="@drawable/ic_qr_code"/>
                <TextView style="@style/Widget.App.CardTitle" android:layout_toStartOf="@id/arrow1" android:layout_toEndOf="@id/icon_mark_attendance" android:text="Mark by QR" />
                <ImageView android:id="@+id/arrow1" style="@style/Widget.App.CardArrow"/>
            </RelativeLayout>
        </com.google.android.material.card.MaterialCardView>

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_mark_by_code"
            style="@style/AdminCardView">
            <RelativeLayout style="@style/Widget.App.CardContent">
                <ImageView style="@style/Widget.App.CardIcon" android:src="@drawable/ic_pin_code"/>
                <TextView style="@style/Widget.App.CardTitle" android:layout_toStartOf="@id/arrow2" android:layout_toEndOf="@id/icon_give_notification" android:text="Mark by Special Code" />
                <ImageView android:id="@+id/arrow2" style="@style/Widget.App.CardArrow"/>
            </RelativeLayout>
        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="8dp"
        app:itemIconTint="@color/bottom_nav_item_tint"
        app:itemTextColor="@color/bottom_nav_item_tint"
        app:itemActiveIndicatorStyle="@style/Widget.App.BottomNavigationView.ActiveIndicator"
        app:menu="@menu/teacher_bottom_nav_menu" />

</RelativeLayout>