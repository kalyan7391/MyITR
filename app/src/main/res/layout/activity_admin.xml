<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:padding="16dp">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurface"
        app:navigationIcon="@drawable/ic_menu"
        app:title="Admin Panel"
        app:titleCentered="true" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Manage Users"
                android:textAppearance="?attr/textAppearanceTitleMedium"
                android:textColor="?attr/colorOnBackground"
                android:paddingTop="16dp"
                android:paddingBottom="8dp" />

            <include layout="@layout/admin_card_item"
                android:id="@+id/card_add_teacher"
                android:layout_marginBottom="8dp" />

            <include layout="@layout/admin_card_item"
                android:id="@+id/card_manage_teachers"
                android:layout_marginBottom="8dp" />

            <include layout="@layout/admin_card_item"
                android:id="@+id/card_add_student"
                android:layout_marginBottom="8dp" />

            <include layout="@layout/admin_card_item"
                android:id="@+id/card_manage_students"
                android:layout_marginBottom="24dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Communication"
                android:textAppearance="?attr/textAppearanceTitleMedium"
                android:textColor="?attr/colorOnBackground"
                android:paddingBottom="8dp" />

            <include layout="@layout/admin_card_item"
                android:id="@+id/card_send_notification"
                android:layout_marginBottom="16dp" />

        </LinearLayout>
    </ScrollView>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/admin_bottom_nav_menu" />

</LinearLayout>